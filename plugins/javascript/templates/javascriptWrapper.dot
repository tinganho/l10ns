
<!--

Else statement

@param {string} string to return

!-->

{{##def.function : load('./function.part'); #}}

;(function() {

  {{=it.localizationMap}}

  {{#def.function}}

  if(typeof require === "function" && typeof exports === 'object' && typeof module === 'object') {
    module.exports = l;
  }
  else if (typeof define === "function" && define.amd) {
    define(function () {
      return l;
    });
  }
  else {
    window.{{=it.functionName}} = l;
  }

})();
