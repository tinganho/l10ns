
<!--

Function

!-->

function gt(key) {
  if(!(key in t)) {
    return 'KEY_NOT_IN_SOURCE: ' + key;
  }
  return t[key].call(undefined, arguments[1]);
};

function lni(operand1, operand2) {
  operand1 = operand1 + '';
  operand2 = operand2 + '';
  operand1LastNumber = operand1.substr(-1,1);
  return operand1LastNumber === operand2;
};
