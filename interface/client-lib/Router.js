
define([

  'backbone'

], function(

  Backbone

) {

  /**
   * We alias the `Backbone.Router` to just `Router`. Because 
   * we wan't to generalize the constructor name.
   *
   * @constructor Router
   */

  return Backbone.Router;

});
